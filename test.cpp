#include "test.h"
#define ADD(a, b) a + b

int main(int argc, char **argv) {
  // NS_ASSERT_M(1 + 1 == 0, "Oh oh...");
  if (1 + 1 == 0) {
  } else {
    return 1298731982;
  };

  return 1 + -1; // ADD(1, -1);
}
